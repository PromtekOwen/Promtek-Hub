# Copy to .dev.vars for local testing with `npm run dev`. Never commit .dev.vars.
JIRA_API_TOKEN=your-jira-api-token
TEMPO_API_TOKEN=your-tempo-api-token
# Pretend to be this person locally (skips Google sign-in). Local only.
DEV_EMAIL=owen.hume@promtek.com
